---
import AppLayout from "../layouts/AppLayout.astro";
import Hero from "../components/Hero.astro"
import CardLeague from "../components/CardLeague.astro";

const response = await fetch('https://apiclient.besoccerapps.com/scripts/api/api.php?key={{APIKEY}}&tz=Europe%2FMadrid&req=categories&filter=my_leagues&format=json')
const data = await response.json()
console.log(data.cards);
---

<AppLayout title="Leagues">
  <Hero 
    image="/images/hero-02.jpg"
    legend="La Europa League ofrece una plataforma para equipos de distintos niveles y tradiciones futbolísticas, lo que la convierte en una competencia emocionante y diversa. El ganador de la Europa League obtiene el derecho de disputar la Supercopa de la UEFA contra el campeón de la Liga de Campeones de la misma temporada."
  />

  <!-- <section class="grid md:grid-cols-2 lg:grid-cols-3 gap-4 p-4">
		{
			data.cards.map(({shortName, logo, flag, name, year}) => (
			<CardLeague 
			shortName={shortName}
			logo={logo}
			flag={flag}
			name={name}
			year={year}
			/>
			))
		}
		</section> -->
</AppLayout>